import { CustomDateAdapterFormatter } from './custom-date-adapter-formatter';

describe('CustomDateAdapterFormatter', () => {
  it('should create an instance', () => {
    expect(new CustomDateAdapterFormatter()).toBeTruthy();
  });
});
